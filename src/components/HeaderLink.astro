---
import type { HTMLAttributes } from 'astro/types';
import { HeaderHref } from './HeaderHref';

type Props = HTMLAttributes<'a'>;

const { href, class: className } = Astro.props;
---

<HeaderHref href={href?.toString() ?? ""} client:load>
	<slot />
</HeaderHref>
